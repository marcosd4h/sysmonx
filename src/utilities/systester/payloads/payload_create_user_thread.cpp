#include "../common.h"
#include "payload_create_user_thread.h"

bool PayloadCreateUserThread::Execute(const std::wstring &payload, const std::wstring &extra)
{
	bool ret = false;

	std::wcout << L"[+] About to execute " << GetDescription() << std::endl;

	return ret;
}